resolver: lts-7.16
compiler: ghc-8.0.2

packages:
- '.'
# - ../machines-io # for testing the build
extra-deps:
  - directory-1.3.0.0
  - machines-io-0.2.0.13

flags: {}
extra-package-dbs: []
nix:
  packages: [zlib]
